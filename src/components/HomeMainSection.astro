---
import copyIcon from "../../public/copy_icon.png";
---

<main class="font-mono">
  <section class="inline-flex flex-col my-12 mx-8 lg:w-1/3 sm:w-2/3">
    <h1 class="text-4xl w-[100%] max-w-2xl">Hi, I'm Scott</h1>
    <p class="w-[100%] max-w-2xl mt-4">
      A fullstack dev with heavy lean toward the frontend. I have 5 years
      experience in many roles and industries. I love to make great experiences
      for users, hopefully adding a bit of joy to a crazy world!
    </p>
  </section>
  <section class="mx-8">
    <h1>Contact me</h1>
    <div class="flex items-center h-10">
      <span class="inline-block email">hello@mscottcameron.com</span>
      <button class="copy-button">
        <img src={copyIcon} class="h-10" />
      </button>
    </div>
  </section>
</main>

<script>
  const button = document.querySelector(".copy-button");
  button?.addEventListener("click", () => {
    const copyContent = document.querySelector(".email")?.innerHTML || "";
    console.log(">>>", copyContent);
    navigator.clipboard.writeText(copyContent);
  });
</script>
